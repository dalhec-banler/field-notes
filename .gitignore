# Flutter/Dart
app/build/
app/.dart_tool/
app/.flutter-plugins*
app/.packages
*.iml
.DS_Store
docs/basemap/
android/key.properties

# Xcode workspace SwiftPM state — regenerated on every build
app/macos/**/xcshareddata/swiftpm/

# Relay (Go)
relay/relay
relay/relay.sqlite*
relay/relay-store/

# local agent instructions, not part of the project
CLAUDE.md
