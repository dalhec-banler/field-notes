# Spec gap review — 2026-08-26

Built state vs `docs/SPEC.md` v0.1, after the 2026-08-26 audit waves 1–3. Read with `AUDIT-2026-08-26.md` (bugs) — this is scope, not bugs.

Legend: ✅ built · 🟡 partial · ❌ not started · 📝 needs a DECISIONS entry

## By milestone (§8)

### M1 — Local core ✅ (acceptance met on emulator + Pixel)
| Spec item | State | Gap |
|---|---|---|
| Schema + migrations | ✅ | Schema v1 only; no migration path yet (fine until v2) |
| Offline basemap (§3.4 loopback + Range) | ✅ | + capture-area MBTiles path; precedence bug H7 open |
| KML/KMZ import w/ review step | ✅ | non-transactional (audit M15) |
| Capture: photo/GPS/notes | ✅ | §7.2 says "voice note" in the flow — ❌ (deps `record`, `speech_to_text` present, unused) |
| Feed, filterable by zone/type/**species/date range/author** | 🟡 | zone + type only |
| Zones w/ point-in-polygon | ✅ | |
| Species library w/ search | ✅ | §7.7 "occurrence counts, **first/last observed, gallery**" — counts only |
| Full export | 🟡 | CSV/GeoJSON/KML/SQLite ✅. Missing: **EXIF GPS written into exported JPEGs** (`native_exif` is a dep, never called), `reports/survival-summary.pdf`, `media/audio/`. Destination is app docs + share-sheet zip, not the §6 shared-storage/MTP folder — 📝 log as a decision (it was agreed in-session on 08-17 but isn't in DECISIONS.md) |
| §7.1 map: layer toggles by type, clustering, **long-press to drop a record** | ❌ | none of the three |
| §7.4 detail: map inset, edit history, related records | ❌ | photo + facts + notes only |

### M2 — Plantings & propagation ✅ (acceptance met)
- Survival denominator: code uses tagged plants, spec §4.8 says `count_planted` (audit M13). Decide + label.
- Alive > planted accepted (audit M13).

### M3 — Features, photo points, tracks ✅ code / 🟡 field
- Ghost overlay: **never field-tested** (M3 acceptance = return a week later, guided within 3 m / 5°). First-visit anchoring bug (audit M14) open.
- Tracks: foreground service now verified on the Pixel (wave 1); no walk test yet.
- §7.8 "time-series scrubber" ❌; §7.9 "maintenance due" ❌ (features have condition logs, no due dates).

### M3.5 — Encrypted backup 🟡
| §11 item | State |
|---|---|
| Incremental, content-addressed, write-once blobs, HMAC names | ✅ |
| Argon2id + XChaCha20-Poly1305 (`cryptography`, D-011) | ✅ |
| Keyring: passphrase + 12-word recovery phrase | ✅ |
| Convenience mode (D-010) | ✅ |
| Local-folder target | ✅ |
| Restore: DB-first, media in background | ✅ — but audit H1/H2 (re-apply loop, sidecar journal) are **open and serious** |
| Verify (manual) | ✅ |
| **Google Drive `drive.appdata` target** | ❌ — blocked on a GCP OAuth client from Austin |
| iCloud / S3 targets | ❌ (S3 is cheap; iCloud is iOS) |
| **Automatic schedule** (charging + Wi-Fi, daily) | ❌ — no `workmanager`; manual only |
| Persistent, resumable upload queue | ❌ (blobs are skipped if present, so re-running resumes at file granularity; no queue across restarts) |
| Secretstream 1 MB chunking | ❌ — whole-file AEAD (fine for photos; matters for >100 MB) |
| Master key in secure storage so routine backups don't prompt | ❌ — `flutter_secure_storage` not a dep; passphrase needed per backup |
| Keep last 5 generations | ❌ dead code (audit M6) |
| Weekly automatic verification | ❌ |
| Prune orphaned blobs (30-day grace) | ❌ |
| Auto Backup exclusion rules | ✅ |
| §11.10 acceptance (factory-reset drill, EXIF round-trip) | 🟡 — lost-phone drill done on host; EXIF can't round-trip because it's never written |

### M4 — Sync & sharing ❌
Nothing started: PowerSync, Supabase, auth, invites UI, RLS, guest links (`precise/fuzzed/zone_only`). Tables (`profiles`, `memberships`, `invites`) exist. Open questions §9.3 and §9.4 are unanswered and only matter here.

### M5 — Intelligence 🟡
- Env-context enrichment (Open-Meteo + NRCS SDA) ✅ — audit M2 (null → 0 mm) open; **Open-Meteo commercial terms unverified** (risk register).
- Pl@ntNet, on-device model, LLM re-rank, BYO API key (secure storage), offline ID queue ❌. `identification_suggestions` table exists; Settings now says "coming later".
- Voice transcription ❌.

### M6 — Programs & devices 🟡
- Programs/practices/activities screens ✅ (basic). Agency export ❌.
- Devices/deployments/detections tables ✅; ingest endpoint ❌ (needs M4 backend).

## Cross-cutting (§7 field ergonomics, §3, §10)
- **Touch targets ≥ 56 dp**: tab bar 68 ✅, FAB ✅, buttons 56–58 ✅; **type/confidence pills are 40–46 dp** ❌; ledger filter chips ~30 dp ❌.
- High-contrast outdoor mode ❌ (density pref glove/dense exists).
- Desktop companion: code ✅, binary ❌ (needs Xcode + CocoaPods).
- Screen sleep between records / thermal: GPS stream now stops when nothing listens (hub) ✅; map keeps a best-accuracy stream alive on every tab (audit L5) 🟡.
- `geolocator` `always` permission only when tracking — we use whileInUse + foreground service ✅.

## Decisions that need logging 📝
1. Export via zip + share sheet instead of the §6 shared-storage/MTP folder (agreed 08-17, not in DECISIONS.md).
2. **Undo hard-deletes** the observation + media (wave 1). §4.1 says soft delete everywhere; this is a deliberate exception for a seconds-old record that never left the phone — needs Austin's sign-off as D-012 or revert to tombstone + media tombstones.
3. Capture auto-flips `general → plant` when a species is chosen; confidence defaults to `certain` on pick.
4. D-001 says app name "Field Notes"; the app presents and is now labelled "Field Station" (design handoff). Reconcile.

## Suggested order
1. Finish the audit's remaining code fixes (H1/H2 restore, H7 tiles, M2 weather) — they're correctness.
2. M1 leftovers that a field user hits daily: long-press to drop a record, feed species/date filters, EXIF GPS in exports (the dep is already there), 56 dp pills.
3. M3.5 to "trustworthy": secure-storage key caching, daily charging+Wi-Fi schedule, generation pruning, weekly verify. Then Drive target once the OAuth client exists.
4. Field test day at SFS: ghost overlay, track logging, capture-area over LTE.
5. Voice notes (deps present) + species first/last/gallery.
6. M4 sync — the big one; don't start until 1–3 are done.
